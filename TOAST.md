# 🔔 Система Toast Уведомлений

Красивые анимированные уведомления для улучшения пользовательского опыта.

## 📋 Типы уведомлений

### ✅ Success (Успех)
- **Цвет**: Зеленый
- **Использование**: Подтверждение успешных действий
- **Примеры**:
  - ✓ Товар добавлен в корзину
  - ✓ Заказ успешно оформлен
  - 💰 Цена снижена
  - 🎉 Скидка увеличена

### ℹ️ Info (Информация)
- **Цвет**: Синий
- **Использование**: Нейтральная информация
- **Примеры**:
  - Товар удален из корзины
  - Цена изменена (нейтрально)

### ⚠️ Warning (Предупреждение)
- **Цвет**: Желтый
- **Использование**: Предупреждения и важные уведомления
- **Примеры**:
  - Введите корректный email
  - Проверьте данные

### ❌ Error (Ошибка)
- **Цвет**: Красный
- **Использование**: Ошибки и проблемы
- **Примеры**:
  - Ошибка оформления заказа
  - Не удалось загрузить данные

## 🎯 Где используются Toast

### 1. Корзина
- ✅ Добавление товара в корзину
- ℹ️ Удаление товара из корзины
- ✅ Увеличение количества существующего товара

### 2. Заказы
- ✅ Успешное оформление заказа
- ❌ Ошибка при оформлении
- ⚠️ Валидация email

### 3. Отслеживание цен
- 💰 Цена снижена (Success)
- 🎉 Скидка увеличена (Success)
- ℹ️ Цена изменена (Info)

## 💻 API Использование

### Базовые методы

```javascript
// Success notification
Toast.success('Заголовок', 'Сообщение', 3000);

// Info notification
Toast.info('Заголовок', 'Сообщение');

// Warning notification
Toast.warning('Заголовок', 'Сообщение');

// Error notification
Toast.error('Заголовок', 'Сообщение', 4000);
```

### Расширенное использование

```javascript
Toast.show({
    type: 'success',      // success, info, warning, error
    title: 'Заголовок',
    message: 'Текст сообщения',
    duration: 3000,       // время показа в мс (0 = бесконечно)
    icon: '🎉'           // кастомная иконка (опционально)
});
```

### Закрытие уведомления

```javascript
const toast = Toast.success('Заголовок', 'Сообщение');
// Закрыть программно
Toast.remove(toast);
```

## 🎨 Стилизация

### CSS классы
- `.toast-container` - контейнер всех toast
- `.toast` - базовый класс уведомления
- `.toast-success` - стиль успеха
- `.toast-info` - стиль информации
- `.toast-warning` - стиль предупреждения
- `.toast-error` - стиль ошибки

### Анимации
- `toast-slide-in` - появление справа
- `toast-slide-out` - исчезновение вправо
- `toast-progress` - прогресс-бар автозакрытия

### Кастомизация через CSS переменные

```css
:root {
    --toast-success: #3fb950;
    --toast-info: #0070d1;
    --toast-warning: #ffbf00;
    --toast-error: #f85149;
}
```

## 🔧 Конфигурация

### Время показа по умолчанию
- **Success**: 3000ms (3 секунды)
- **Info**: 3000ms
- **Warning**: 3000ms
- **Error**: 4000ms (дольше для критичных сообщений)

### Позиция
- **Desktop**: Верхний правый угол (top: 80px, right: 20px)
- **Mobile**: Верх по центру (top: 70px, left/right: 10px)

### Максимальное количество
Неограничено, все toast показываются стеком вертикально с отступом 12px

## 🌙 Поддержка тем

Toast адаптируются под темную/светлую тему автоматически через CSS переменные:

- `--card-bg` - фон toast
- `--text-primary` - цвет заголовка
- `--text-secondary` - цвет текста
- `--border-color` - цвет рамки

## 📱 Адаптивность

### Desktop
- Ширина: 280-400px
- Позиция: справа
- Анимация: слайд справа

### Mobile (< 480px)
- Ширина: на всю ширину минус отступы
- Позиция: сверху по центру
- Анимация: слайд сверху

## 🎭 Взаимодействие

- **Hover**: Легкий сдвиг влево + усиление тени
- **Клик на крестик**: Мгновенное закрытие
- **Auto-close**: Закрытие через duration миллисекунд
- **Progress bar**: Визуальный индикатор оставшегося времени

## 🔄 Автоматическое обновление цен

Система автоматически проверяет изменения цен каждые 5 минут и показывает toast:

```javascript
// В init()
setInterval(async () => {
    const response = await fetch('/api/products');
    const products = await response.json();
    checkPriceChanges(products); // Автоматически покажет toast
}, 5 * 60 * 1000);
```

## 📊 Примеры использования

### Добавление в корзину
```javascript
app.addToCart = function(productId) {
    const product = findProductById(productId);
    const existingItem = app.cart.find(item => item.id === productId);
    
    if (existingItem) {
        existingItem.quantity++;
        Toast.success('Количество увеличено', 
            `${product.name} теперь в корзине (${existingItem.quantity} шт.)`);
    } else {
        app.cart.push({ ...product, quantity: 1 });
        Toast.success('Добавлено в корзину', product.name);
    }
};
```

### Удаление из корзины
```javascript
app.removeFromCart = function(productId) {
    const product = app.cart.find(item => item.id === productId);
    app.cart = app.cart.filter(item => item.id !== productId);
    
    if (product) {
        Toast.info('Удалено из корзины', product.name);
    }
};
```

### Валидация формы
```javascript
if (!email || !email.includes('@')) {
    Toast.warning('Введите корректный email', 
        'Email обязателен для получения кодов');
    return;
}
```

### Успешный заказ
```javascript
if (result.success) {
    Toast.success('✅ Заказ оформлен!', 
        `Коды отправлены на ${email}`, 4000);
    // Очистка корзины...
}
```

### Ошибка заказа
```javascript
catch (error) {
    Toast.error('Ошибка оформления заказа', 
        'Попробуйте снова', 4000);
}
```

## 🚀 Преимущества

1. **Ненавязчивые** - не блокируют интерфейс
2. **Красивые** - плавные анимации и современный дизайн
3. **Информативные** - заголовок + описание + иконка
4. **Адаптивные** - работают на всех устройствах
5. **Тематические** - поддержка светлой/темной темы
6. **Доступные** - можно закрыть вручную или авто-закрытие
7. **Легковесные** - чистый JavaScript без зависимостей

## 📝 Лучшие практики

1. **Краткие заголовки** (2-4 слова)
2. **Понятные сообщения** (до 1 строки)
3. **Правильные типы** (success для позитива, error для ошибок)
4. **Адекватное время** (3-4 секунды обычно достаточно)
5. **Не переусердствовать** - показывайте toast только для важных событий
6. **Иконки по смыслу** - используйте эмодзи для дополнительной выразительности

## 🐛 Дебаг

Проверить работу toast в консоли браузера:

```javascript
// Тест всех типов
Toast.success('Успех', 'Операция выполнена');
Toast.info('Информация', 'Что-то произошло');
Toast.warning('Внимание', 'Проверьте данные');
Toast.error('Ошибка', 'Что-то пошло не так');

// Кастомный toast
Toast.show({
    type: 'success',
    title: 'Тест',
    message: 'Это тестовое сообщение',
    duration: 5000,
    icon: '🎉'
});
```

---

**Автор**: INSIDER PlayStation Team  
**Версия**: 2.0.0  
**Дата**: Февраль 2026
