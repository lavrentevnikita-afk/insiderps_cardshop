# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ЗАПУСКУ

## 🎯 ШАГ 1: Установка Node.js (если еще не установлен)

1. Перейдите на https://nodejs.org/
2. Скачайте LTS версию для Windows
3. Установите, следуя инструкциям
4. Проверьте установку в PowerShell:
   ```
   node --version
   npm --version
   ```

## 🤖 ШАГ 2: Создание Telegram бота

### 2.1 Создание бота
1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте команду `/newbot`
4. Введите имя бота (например: "My Shop Bot")
5. Введите юзернейм бота (должен заканчиваться на "bot", например: "myshop_bot")
6. **СОХРАНИТЕ токен бота** - он понадобится позже!

Пример токена: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2.2 Настройка описания (опционально)
```
/setdescription
<выберите своего бота>
<введите описание>
```

### 2.3 Настройка команд
```
/setcommands
<выберите своего бота>
start - Начать работу с ботом
shop - Открыть магазин
help - Помощь
```

## 💳 ШАГ 3: Настройка платежей

### 3.1 Для тестирования (ЮKassa Test)
1. В @BotFather отправьте `/mybots`
2. Выберите своего бота
3. Нажмите "Payments"
4. Выберите "ЮKassa (Test mode)"
5. Введите тестовый токен: `381764678:TEST:000000000_F91D8F69C042267444B74CC0B3C747757776B`
6. **СОХРАНИТЕ этот токен!**

### 3.2 Для продакшена (реальные платежи)
1. Зарегистрируйтесь на https://yookassa.ru/
2. Получите боевой токен
3. Добавьте его в @BotFather через "Payments"

**Поддерживаемые платежные системы:**
- ЮKassa (Россия)
- Stripe (международные)
- Другие по документации Telegram

## 💻 ШАГ 4: Установка проекта

### 4.1 Установка зависимостей
Откройте PowerShell в папке проекта:
```powershell
cd C:\Users\Nikita\Desktop\psshop
npm install
```

### 4.2 Создание .env файла
1. Скопируйте файл `.env.example` и переименуйте в `.env`
2. Откройте `.env` в блокноте
3. Заполните данные:

```env
# Токен вашего бота из @BotFather
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Токен платежной системы
PAYMENT_TOKEN=381764678:TEST:000000000_F91D8F69C042267444B74CC0B3C747757776B

# URL где будет размещен ваш Web App (пока оставьте как есть)
WEBAPP_URL=http://localhost:3000

# Порт сервера
PORT=3000

# ID админа (ваш Telegram ID - опционально)
ADMIN_ID=123456789
```

**Как узнать свой Telegram ID:**
1. Напишите боту @userinfobot
2. Скопируйте ваш ID

## 🛍 ШАГ 5: Настройка товаров и ключей

### 5.1 Настройка товаров
Откройте `data/products.json` и отредактируйте товары:

```json
[
  {
    "id": "product_1",
    "name": "Название товара",
    "description": "Описание товара",
    "price": 299,
    "currency": "RUB",
    "image": "https://ссылка-на-картинку.jpg"
  }
]
```

**Важно:**
- `id` - уникальный идентификатор (латиницей)
- `price` - цена в рублях (целое число)
- `currency` - валюта (RUB, USD, EUR)
- `image` - ссылка на картинку товара

### 5.2 Добавление ключей
Откройте `data/keys.json` и добавьте ключи для каждого товара:

```json
{
  "product_1": [
    "KEY-XXXX-XXXX-XXXX-0001",
    "KEY-XXXX-XXXX-XXXX-0002",
    "KEY-XXXX-XXXX-XXXX-0003"
  ],
  "product_2": [
    "KEY-YYYY-YYYY-YYYY-0001",
    "KEY-YYYY-YYYY-YYYY-0002"
  ]
}
```

**Важно:** ID товара должен совпадать с ID в `products.json`!

## 🚀 ШАГ 6: Локальное тестирование

### 6.1 Запуск бота
Откройте PowerShell в папке проекта:
```powershell
npm start
```

Вы должны увидеть:
```
🤖 Бот запущен!
```

### 6.2 Тестирование бота
1. Откройте Telegram
2. Найдите своего бота по юзернейму
3. Отправьте `/start`
4. Проверьте, что бот отвечает

**Что должно работать на этом этапе:**
- ✅ Команды `/start`, `/help`, `/shop`
- ✅ Кнопки в сообщениях
- ❌ Web App пока не будет работать (нужен хостинг)

### 6.3 Просмотр Web App локально
Откройте второй терминал:
```powershell
node server/index.js
```

Откройте браузер: http://localhost:3000

## 🌐 ШАГ 7: Деплой Web App

### Вариант А: Vercel (Рекомендуется)

1. **Регистрация:**
   - Перейдите на https://vercel.com
   - Зарегистрируйтесь через GitHub

2. **Деплой:**
   - Нажмите "New Project"
   - Выберите папку `webapp`
   - Нажмите "Deploy"
   - Получите URL (например: `https://myshop-abc123.vercel.app`)

3. **Настройка:**
   - Скопируйте полученный URL
   - Обновите `.env`: `WEBAPP_URL=https://myshop-abc123.vercel.app`

### Вариант Б: GitHub Pages

1. **Создание репозитория:**
   - Создайте новый репозиторий на GitHub
   - Загрузите папку `webapp`

2. **Настройка Pages:**
   - Settings → Pages
   - Source: main branch / root
   - Сохраните и получите URL

3. **Обновление .env:**
   - `WEBAPP_URL=https://username.github.io/psshop`

### Вариант В: Netlify

1. Зарегистрируйтесь на https://netlify.com
2. Drag & drop папку `webapp`
3. Получите URL
4. Обновите `.env`

## 🔗 ШАГ 8: Подключение Web App к боту

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите своего бота
4. Нажмите "Bot Settings"
5. Нажмите "Menu Button"
6. Выберите "Configure Menu Button"
7. Введите текст кнопки: "🛍 Открыть магазин"
8. Введите URL вашего Web App: `https://your-webapp-url.com`

**Проверка:**
- Откройте своего бота в Telegram
- Нажмите на кнопку меню (рядом с полем ввода)
- Должен открыться ваш магазин

## ☁️ ШАГ 9: Деплой бота на сервер

### Вариант А: Railway (Рекомендуется)

1. **Регистрация:**
   - Перейдите на https://railway.app
   - Зарегистрируйтесь через GitHub

2. **Создание проекта:**
   - New Project → Deploy from GitHub repo
   - Выберите репозиторий
   - Railway автоматически определит Node.js

3. **Настройка переменных:**
   - Variables → Add Variables
   - Добавьте все переменные из `.env`:
     - `BOT_TOKEN`
     - `PAYMENT_TOKEN`
     - `WEBAPP_URL`
     - `PORT` (Railway установит автоматически)
     - `ADMIN_ID`

4. **Деплой:**
   - Deploy → Deploy Now
   - Бот автоматически запустится!

### Вариант Б: Heroku

1. Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
2. Команды:
```powershell
heroku login
heroku create myshop-bot
git init
git add .
git commit -m "Initial commit"
git push heroku main
heroku config:set BOT_TOKEN=ваш_токен
heroku config:set PAYMENT_TOKEN=ваш_payment_токен
heroku config:set WEBAPP_URL=ваш_webapp_url
```

### Вариант В: VPS (для продвинутых)

1. Арендуйте VPS (например, на DigitalOcean)
2. Установите Node.js
3. Загрузите проект через git или FTP
4. Установите PM2: `npm install -g pm2`
5. Запустите: `pm2 start bot/index.js --name shop-bot`
6. Автозапуск: `pm2 startup` и `pm2 save`

## ✅ ШАГ 10: Финальное тестирование

### 10.1 Тестовая покупка
1. Откройте бота в Telegram
2. Нажмите "Открыть магазин"
3. Выберите любой товар
4. Нажмите "Купить"
5. Используйте тестовую карту:
   - Номер: `5555 5555 5555 4444`
   - Срок: любая будущая дата
   - CVC: `123`
6. Подтвердите оплату
7. Проверьте получение ключа

### 10.2 Проверка выдачи ключей
- Ключ должен прийти сразу после оплаты
- Ключ должен быть из файла `keys.json`
- Использованный ключ должен удалиться из базы

### 10.3 Проверка истории заказов
Откройте файл `data/orders.json` - там должна быть запись о заказе.

## 🎉 ШАГ 11: Переход в продакшен

### 11.1 Замена тестового payment token
1. Получите боевой токен от платежной системы
2. Обновите в Railway/Heroku:
   ```
   PAYMENT_TOKEN=боевой_токен
   ```

### 11.2 Добавление реальных товаров
- Обновите `products.json`
- Добавьте реальные изображения
- Установите правильные цены

### 11.3 Добавление реальных ключей
- Сгенерируйте ключи для ваших продуктов
- Добавьте их в `keys.json`

### 11.4 Проверка безопасности
- [ ] `.env` файл не в git репозитории
- [ ] Payment token защищен
- [ ] Доступ к `/api/orders` ограничен
- [ ] HTTPS на Web App
- [ ] Бэкапы данных настроены

## 📊 ШАГ 12: Мониторинг

### 12.1 Проверка логов
Railway/Heroku:
- Откройте Logs в панели управления
- Следите за ошибками

PM2 (VPS):
```powershell
pm2 logs shop-bot
```

### 12.2 Проверка данных
- `data/orders.json` - история продаж
- `data/keys.json` - остаток ключей

### 12.3 Уведомления админу
Если указали `ADMIN_ID` в `.env`, админ будет получать:
- Уведомления о новых продажах
- Уведомления об ошибках (нет ключей и т.д.)

## 🆘 Решение проблем

### Бот не запускается
```
❌ Ошибка: BOT_TOKEN не найден
```
**Решение:** Проверьте `.env` файл, убедитесь что токен правильный.

### Web App не открывается
**Решение:**
- Проверьте что URL в @BotFather правильный
- Убедитесь что сайт работает (откройте в браузере)
- Проверьте что используется HTTPS (не HTTP)

### Платежи не работают
**Решение:**
- Проверьте `PAYMENT_TOKEN` в `.env`
- Убедитесь что платежная система активирована в @BotFather
- Для теста используйте тестовый токен

### Ключи не выдаются
**Решение:**
- Проверьте `data/keys.json`
- Убедитесь что `product_id` совпадает
- Проверьте что есть доступные ключи для товара

### "Cannot find module"
**Решение:**
```powershell
rm -rf node_modules
rm package-lock.json
npm install
```

## 📈 Дальнейшее развитие

### Фича 1: Админ-панель
- Управление товарами через бота
- Загрузка ключей через бота
- Статистика продаж

### Фича 2: База данных
- Переход с JSON на PostgreSQL/MongoDB
- Более надежное хранение данных
- Поддержка больших объемов

### Фича 3: Дополнительные функции
- Реферальная система
- Промокоды и скидки
- История покупок для пользователя
- Возврат средств

### Фича 4: Улучшение UX
- Категории товаров
- Поиск по товарам
- Отзывы
- Избранное

## 📞 Поддержка

**Документация Telegram:**
- Боты: https://core.telegram.org/bots
- Web Apps: https://core.telegram.org/bots/webapps
- Payments: https://core.telegram.org/bots/payments

**Полезные ресурсы:**
- Node.js: https://nodejs.org/docs
- Express: https://expressjs.com
- Railway: https://docs.railway.app

---

## ✅ Чеклист готовности к запуску

- [ ] Node.js установлен
- [ ] Бот создан через @BotFather
- [ ] Токен бота получен
- [ ] Payment token получен
- [ ] Зависимости установлены (`npm install`)
- [ ] `.env` файл настроен
- [ ] Товары добавлены в `products.json`
- [ ] Ключи добавлены в `keys.json`
- [ ] Web App задеплоен на хостинг
- [ ] Web App подключен к боту через @BotFather
- [ ] Бот задеплоен на сервер
- [ ] Тестовая покупка проведена успешно
- [ ] Ключ получен после оплаты
- [ ] Логи проверены, ошибок нет

**Когда все пункты выполнены - бот готов к работе! 🎉**
