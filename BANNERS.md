# 📢 Управление баннерами

Полная система управления промо-баннерами на главной странице магазина.

## 🎯 Возможности

- ➕ Создание новых баннеров
- ✏️ Редактирование текста
- 🖼️ Загрузка изображений (в разработке)
- 🔗 Добавление ссылок
- 🔘 Включение/отключение баннеров
- 📊 Управление порядком отображения
- 🗑️ Удаление баннеров

## 📋 Команды

### /banners
Открывает панель управления баннерами со списком всех существующих баннеров.

```
Использование: /banners
```

**Отображает:**
- Список всех баннеров с их статусом (🟢 активен / 🔴 отключен)
- Заголовок и подзаголовок каждого баннера
- Наличие изображения и ссылки
- Порядок отображения

## 🎨 Создание баннера

### Шаг 1: Добавление
1. Отправьте команду `/banners`
2. Нажмите кнопку **"➕ Добавить новый баннер"**

### Шаг 2: Заголовок
Отправьте заголовок для баннера (например: "СКИДКИ ДО 50%")

### Шаг 3: Подзаголовок
Отправьте подзаголовок (например: "Только сегодня!")

### Результат
Баннер будет создан и автоматически включен. Вы сможете:
- Добавить изображение
- Добавить ссылку
- Изменить порядок отображения
- Настроить другие параметры

## ⚙️ Настройка баннера

### Просмотр баннера
Нажмите на баннер в списке, чтобы открыть панель управления.

### Доступные действия

#### ✅ Включить / ❌ Отключить
Переключает видимость баннера на сайте.
- **Включен** (🟢): Баннер отображается на главной странице
- **Отключен** (🔴): Баннер скрыт от пользователей

#### ✏️ Редактировать
Изменить заголовок и подзаголовок баннера.

#### 🖼️ Изменить фото
Загрузить изображение для баннера (в разработке).

#### 🔗 Изменить ссылку
Добавить или изменить URL, по которому откроется баннер при клике.

**Формат ссылки:**
```
https://example.com
```

#### 📊 Изменить порядок
Установить позицию баннера в списке (чем меньше число, тем выше приоритет).

#### 🗑️ Удалить баннер
Полностью удалить баннер из системы (требуется подтверждение).

## 📁 Структура данных

### Файл: data/banners.json

```json
[
  {
    "id": 1,
    "enabled": true,
    "title": "НЕ ХВАТАЕТ ПРИМОГЕМОВ?",
    "subtitle": "САМЫЕ НИЗКИЕ ЦЕНЫ!",
    "image": null,
    "link": null,
    "order": 1
  }
]
```

### Поля баннера

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | number | Уникальный идентификатор баннера |
| `enabled` | boolean | Активен ли баннер |
| `title` | string | Заголовок баннера |
| `subtitle` | string | Подзаголовок/описание |
| `image` | string\|null | URL изображения |
| `link` | string\|null | Ссылка для перехода |
| `order` | number | Порядок отображения (1 = первый) |

## 🌐 Отображение на сайте

Баннеры загружаются через API: `GET /api/banners`

### Логика отображения
1. Загружаются только **активные** баннеры (`enabled: true`)
2. Сортируются по полю `order` (по возрастанию)
3. На главной странице показывается **первый** баннер из списка

### Пример кода (webapp)
```javascript
async function loadPromoBanners() {
    const response = await fetch('/api/banners');
    const banners = await response.json();
    
    if (banners.length > 0) {
        const banner = banners[0]; // Первый активный баннер
        // Отображаем banner.title и banner.subtitle
    }
}
```

## 🔄 Синхронизация

Все изменения баннеров автоматически:
- ✅ Сохраняются в `data/banners.json`
- ✅ Логируются в `data/logs.json`
- ✅ Синхронизируются с GitHub (автокоммит)
- ✅ Обновляются на сайте в реальном времени

## 📊 Примеры использования

### Пример 1: Акционный баннер
```
Заголовок: СКИДКИ ДО 50%
Подзаголовок: На все карты PS Plus
Ссылка: https://insiderplaystation.ru/catalog/usa
Порядок: 1
```

### Пример 2: Промокод
```
Заголовок: ПРОМОКОД INSIDER2024
Подзаголовок: -10% на первый заказ
Ссылка: null
Порядок: 2
```

### Пример 3: Новинка
```
Заголовок: НОВИНКА! GTA 6
Подзаголовок: Предзаказ уже доступен
Ссылка: https://insiderplaystation.ru/products/gta6
Порядок: 3
```

## 🎯 Best Practices

### Заголовки
- ✅ Короткие (2-5 слов)
- ✅ Привлекающие внимание
- ✅ КАПСЛОК для важных акций
- ❌ Слишком длинные заголовки

### Подзаголовки
- ✅ Дополняют заголовок
- ✅ Конкретная информация
- ✅ Call to action
- ❌ Дублирование заголовка

### Порядок
- **1** - Самые важные акции
- **2-5** - Регулярные промо
- **10+** - Дополнительная информация

### Количество баннеров
- Рекомендуется: **2-5 активных** баннеров
- Показывается: **1 баннер** на главной
- Оптимально: **3 баннера** (можно ротировать)

## 🔒 Безопасность

### Права доступа
Только администраторы могут:
- Создавать баннеры
- Редактировать баннеры
- Удалять баннеры

### Проверка прав
```javascript
if (!isAdmin(userId)) {
    bot.sendMessage(chatId, '⛔ У вас нет доступа к этой команде');
    return;
}
```

## 📝 Логирование

Все действия с баннерами логируются:

```javascript
logAction('ADD_BANNER', userId, { bannerId: 1, title: "Скидки" });
logAction('TOGGLE_BANNER', userId, { bannerId: 1, enabled: true });
logAction('DELETE_BANNER', userId, { bannerId: 1, title: "Скидки" });
```

**Лог-файл:** `data/logs.json`

## 🚀 Развитие функционала

### Запланировано
- [ ] Загрузка изображений через бот
- [ ] Расписание показа баннеров
- [ ] A/B тестирование баннеров
- [ ] Статистика кликов по баннерам
- [ ] Ротация нескольких баннеров
- [ ] Таргетинг по регионам

### Возможности
- [ ] Видео-баннеры
- [ ] Анимированные баннеры
- [ ] Персонализация для пользователей
- [ ] Баннеры в каталоге

## 🆘 Troubleshooting

### Баннер не отображается
1. Проверьте, что баннер **включен** (`enabled: true`)
2. Проверьте `order` - баннер с меньшим числом показывается первым
3. Обновите страницу (Ctrl+F5)
4. Проверьте консоль браузера на ошибки

### Изменения не применяются
1. Проверьте логи бота: `pm2 logs bot --lines 50`
2. Убедитесь, что файл `data/banners.json` обновлен
3. Проверьте API: `curl https://insiderplaystation.ru/api/banners`
4. Синхронизируйте с GitHub вручную

### Права доступа
```bash
# Проверить ADMIN_ID
docker exec psshop-bot node -e "require('dotenv').config(); console.log(process.env.ADMIN_ID)"

# Ваш Telegram ID можно узнать через бот @userinfobot
```

---

**Автор**: INSIDER PlayStation Team  
**Версия**: 2.1.0  
**Дата**: Февраль 2026
