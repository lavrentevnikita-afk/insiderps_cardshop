# 🚀 Оптимизация производительности

Документация по реализованным оптимизациям для улучшения скорости загрузки и UX.

## 📊 Результаты оптимизации

### Минификация файлов
- **JavaScript**: 42.71 KB → **27.33 KB** (экономия **36%**)
- **CSS**: 44.30 KB → **31.34 KB** (экономия **29%**)
- **Общая экономия трафика**: ~28 KB на загрузку

### Lazy Loading изображений
- Изображения загружаются только при приближении к viewport
- Экономия трафика при первой загрузке: **~80-90%**
- Ускорение первой отрисовки страницы (FCP)

### Preloader
- Плавная загрузка с анимированным логотипом
- Скрывается автоматически после полной загрузки страницы
- Улучшает восприятие скорости загрузки

## 🎨 Preloader

### Особенности
- **Анимированный логотип** PlayStation с pulse эффектом
- **Вращающийся spinner** с брендовыми цветами
- **Плавное исчезновение** через 300-500ms после загрузки
- **Поддержка тем** - адаптируется под светлую/темную тему

### Компоненты
```html
<div id="preloader" class="preloader">
    <div class="preloader-content">
        <div class="preloader-logo">
            <img src="PlayStation_logo.svg" alt="PlayStation">
        </div>
        <div class="preloader-spinner"></div>
        <div class="preloader-text">INSIDER PlayStation</div>
    </div>
</div>
```

### Анимации
- `preloader-pulse` - пульсация логотипа (2s infinite)
- `preloader-spin` - вращение spinner (1s linear)
- `preloader-glow` - мерцание текста с цветом бренда

### Автоматическое скрытие
```javascript
window.addEventListener('load', function() {
    const preloader = document.getElementById('preloader');
    setTimeout(() => {
        preloader.classList.add('loaded'); // opacity: 0
        setTimeout(() => preloader.remove(), 500);
    }, 300);
});
```

## 🖼️ Lazy Loading изображений

### Реализация

**HTML атрибут:**
```html
<img src="image.jpg" alt="Product" loading="lazy">
```

**JavaScript enhancement:**
```javascript
function initLazyLoading() {
    const images = document.querySelectorAll('img[loading="lazy"]');
    
    const imageObserver = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const img = entry.target;
                img.addEventListener('load', () => {
                    img.classList.add('loaded'); // opacity: 1
                });
                observer.unobserve(img);
            }
        });
    }, {
        rootMargin: '50px' // Загружать за 50px до появления
    });
    
    images.forEach(img => imageObserver.observe(img));
}
```

### CSS анимация
```css
img[loading="lazy"] {
    opacity: 0;
    transition: opacity 0.4s ease-in-out;
}

img[loading="lazy"].loaded {
    opacity: 1;
}
```

### Где применяется
- ✅ Карточки товаров в каталоге
- ✅ Популярные товары на главной
- ✅ Детальная страница товара
- ✅ Изображения в промо-баннерах

### Преимущества
1. **Экономия трафика** - загружаются только видимые изображения
2. **Быстрая первая загрузка** - меньше данных для Initial Load
3. **Лучший UX** - страница быстрее становится интерактивной
4. **Плавное появление** - fade-in эффект при загрузке

## 📦 Минификация

### Установка инструментов
```bash
npm install --save-dev terser clean-css-cli
```

### NPM Scripts

**package.json:**
```json
{
  "scripts": {
    "minify:js": "terser webapp/app.js -o webapp/app.min.js -c -m",
    "minify:css": "cleancss -o webapp/style.min.css webapp/style.css",
    "minify": "npm run minify:js && npm run minify:css",
    "build": "npm run minify"
  }
}
```

### Команды
```bash
# Минификация всего
npm run minify

# Только JavaScript
npm run minify:js

# Только CSS  
npm run minify:css

# Build для production
npm run build
```

### Что происходит

**JavaScript (terser):**
- Удаление комментариев
- Удаление пробелов и переносов строк
- Сокращение имен переменных
- Оптимизация кода

**CSS (clean-css):**
- Удаление комментариев
- Удаление пробелов
- Объединение правил
- Минификация селекторов

### .gitignore
Минифицированные файлы не коммитятся:
```gitignore
# Minified files (generated on build)
webapp/*.min.js
webapp/*.min.css
```

### Использование в production

**index.html (для production):**
```html
<!-- Development -->
<link rel="stylesheet" href="style.css">
<script src="app.js"></script>

<!-- Production -->
<link rel="stylesheet" href="style.min.css">
<script src="app.min.js"></script>
```

## 🎯 Метрики производительности

### До оптимизации
- **Размер JS**: 42.71 KB
- **Размер CSS**: 44.30 KB
- **Общий размер**: 87.01 KB
- **Загрузка всех изображений**: ~500-800 KB

### После оптимизации
- **Размер JS**: 27.33 KB (-36%)
- **Размер CSS**: 31.34 KB (-29%)
- **Общий размер**: 58.67 KB
- **Экономия**: **28.34 KB** (32.5%)
- **Загрузка изображений**: только видимые (~50-150 KB)

### Ожидаемые улучшения
- ⚡ **FCP** (First Contentful Paint): -30-40%
- ⚡ **LCP** (Largest Contentful Paint): -20-30%
- 📈 **TTI** (Time to Interactive): -25-35%
- 📊 **Lighthouse Score**: +10-15 points

## 🔧 Дополнительные оптимизации

### Выполнено ✅
- [x] Минификация JavaScript
- [x] Минификация CSS
- [x] Lazy loading изображений
- [x] Preloader с анимацией
- [x] CSS transitions и animations
- [x] Оптимизация селекторов

### Рекомендуется (опционально)
- [ ] Gzip/Brotli сжатие на сервере
- [ ] HTTP/2 для параллельной загрузки
- [ ] CDN для статических файлов
- [ ] WebP формат для изображений
- [ ] Service Worker для кеширования
- [ ] Code splitting для больших приложений

## 📝 Лучшие практики

### При разработке
1. Используйте оригинальные файлы (`app.js`, `style.css`)
2. Комментируйте код - комментарии удаляются при минификации
3. Пишите читаемый код - минификатор оптимизирует

### Перед деплоем
1. Запустите `npm run build`
2. Проверьте, что `.min` файлы созданы
3. Переключите HTML на минифицированные версии
4. Протестируйте функциональность

### На production
1. Используйте только `.min` версии файлов
2. Включите Gzip на веб-сервере (Nginx/Apache)
3. Настройте кеширование статики
4. Мониторьте метрики производительности

## 🌐 Настройка сервера

### Nginx
```nginx
# Gzip compression
gzip on;
gzip_vary on;
gzip_min_length 256;
gzip_types text/css application/javascript application/json image/svg+xml;

# Browser caching
location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### Express.js
```javascript
const compression = require('compression');
app.use(compression());

// Static files caching
app.use(express.static('webapp', {
    maxAge: '1y',
    immutable: true
}));
```

## 🐛 Troubleshooting

### Минификация не работает
```bash
# Проверьте установку пакетов
npm list terser clean-css-cli

# Переустановите если нужно
npm install --save-dev terser clean-css-cli
```

### Lazy loading не работает в старых браузерах
```javascript
// Fallback уже реализован
if ('IntersectionObserver' in window) {
    // Modern browsers
} else {
    // Fallback для старых браузеров
}
```

### Preloader зависает
```javascript
// Принудительное скрытие через 5 секунд
setTimeout(() => {
    const preloader = document.getElementById('preloader');
    if (preloader) preloader.remove();
}, 5000);
```

## 📊 Мониторинг

### Chrome DevTools
1. Откройте DevTools (F12)
2. Network tab → Проверьте размеры файлов
3. Performance tab → Запишите загрузку страницы
4. Lighthouse → Запустите аудит

### Метрики для отслеживания
- **Load Time**: общее время загрузки
- **DOM Content Loaded**: время до интерактивности
- **First Paint**: первая отрисовка
- **Images Loaded**: количество загруженных изображений
- **Total Transfer**: общий объем переданных данных

---

**Автор**: INSIDER PlayStation Team  
**Версия**: 2.0.0  
**Дата**: Февраль 2026
